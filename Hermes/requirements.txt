# MAO.ai Hermes Gateway — Python 3.12 (Docker python:3.12-slim)
# Pin exact versions for reproducibility in Docker

fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2

# LLM client (OpenRouter uses OpenAI-compatible API)
openai==1.55.0

# Retry helpers — used by services.llm_router.call_llm
tenacity>=8.0.0

# Database
asyncpg>=0.30.0
sqlalchemy==2.0.35
alembic==1.13.3

# Cache
redis==5.1.0

# Vector DB
qdrant-client==1.12.0

# Self-hosted embeddings (Phase 3A → BGE-M3 default).
# fastembed runs ONNX models locally (~568 MB BGE-M3, RU support, FREE).
# Replaces OpenAI text-embedding-3-small as the default Legal RAG provider.
fastembed==0.4.2

# Config / validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Encryption
cryptography==43.0.1

# Object Storage (MinIO / S3) — Visual Agent assets
boto3==1.35.36

# Env
python-dotenv==1.0.1
