"""
Hermes Gateway routers.

Routers are imported by main.py via `from routers import health, skills, agent`.
This package marker has no re-exports — each submodule is imported by name
to keep import errors loud and explicit on startup.
"""
