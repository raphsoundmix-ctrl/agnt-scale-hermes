// Add to src/lib/env.ts server block:
HERMES_URL: z.string().url().optional(),
HERMES_INTERNAL_TOKEN: z.string().min(32).optional(),

// runtimeEnv:
HERMES_URL: process.env.HERMES_URL,
HERMES_INTERNAL_TOKEN: process.env.HERMES_INTERNAL_TOKEN,
